import 'package:flutter/material.dart';

class Tugas6hal2 extends StatelessWidget {
  const Tugas6hal2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Halaman 2")),
      body: const Center(child: Text("Hai, ini halaman 2!")),
    );
  }
}
